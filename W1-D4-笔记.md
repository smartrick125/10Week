# Week 1 · Day 4 笔记（2026-09-22 · L3 下 RG API + Render Graph Viewer）

**今日判据**：在 Render Graph Viewer 里**指着自己的管线**说出四条机制哪条生效了、哪条没生效。
**今天要还三笔账**：钩子② / 钩子③ / 机制①②③ 的证据
**工程**：`F:\unity\TestProject`

---

## 步骤 0 · D3 判据重默（15 分钟）

合上 D3 笔记，四条各一句（重点默生命周期那条，昨天错过一次）：

- 裁剪：解决pass执行输出但是最后没人读取，依赖于DAG回溯，防止白白浪费GPU时间。
- 生命周期：解决人肉管理RT导致的内存泄露，RenderGraph自动检测 RT首个被pass写入前申请内存 最后一个pass读取后释放
- 别名：解决显存峰值问题 根据生命周期不重叠 相同属性RT可以使用同一块显存
- 合并：解决带宽问题 依赖Attachment判断相同的pass放入同一个native render pass在移动端图块延迟模式下 减少load store操作 让上一个pass的结果保存在tile memoty中实现合并减少带宽。

---

## 步骤 1 · Render Graph Viewer 实操（先看见，再读文档）

`Window → Analysis → Rendering Debugger`？不对 → **`Window → Analysis → Render Graph Viewer`**

### 1.1 窗口长什么样（先描述，别急着找答案）

- 横轴是：pass，按执行顺序从左到右
- 每一行是：一个资源 贴图/buffer
- 颜色/条块代表：这个资源子在这个pass中是 被读 还是 被写

- 虚线：资源 没有被分配/已经释放 在生命周期之外
- 灰块：活着但是这个pass没有碰它
- 红块：写(Write)
- 绿块：读(read)
- 半红半绿：同一个pass又读又写
- 球体网络图标：资源在这里被创建导入
### 1.2 还债 · 机制①裁剪的证据

**钩子③**：没有透明物体时，透明那一格在 Frame Debugger 里整格消失。
在 Viewer 里它是被标为 culled，还是根本没出现，还是出现了但空？

>出现了证据是在render graph viewer中看到了Draw Transparent Objects的pass到那时frame debugger中没有只能说明pass生效了但是没画

**结论**：机制① 在我的管线里 没生效
看不到(Draw Transparent Objects 在图里没被裁)

### 1.3 还债 · 机制②生命周期的证据
证据位置：Redner Graph Viewer
找到 `_BlurTexture`：

- 首次写它的 pass：Vertical Blur Render Pass
- 末次读它的 pass：Horizontal Blur Render Pass
- 它的生命周期横跨几个 pass：；两个

**结论**：机制② 在我的管线里 生效

### 1.4 还债 · 机制③别名的证据

`_BlurTexture` 有没有和别的 RT 共用同一块内存？

>看不到

**结论**：机制③ 看不到

### 1.5 还债 · 钩子②（RP 编号跳过 4）

>详见 D3 步骤 3 钩子②

### 1.6 还债 · 机制④合并的证据（Render Graph Viewer）

**① 合并生效了**（Pass List → `Draw Opaque Objects, Draw Skybox` 展开）：

> Native Pass was created by merging 2 Raster Render Passes.

**② 合并省了什么**（同一组的 `Attachment Load/Store Actions`）：

```
_CameraDepthAttachment   Load: Clear  →  Store: Store
_CameraTargetAttachment  Load: Clear  →  Store: Store
```

整个合并组**只有一组** Load/Store：开头 Clear 一次、结尾 Store 一次。
两个 subpass（Opaque / Skybox）中间**零次 store-load 往返**，数据全程留在 tile memory。

> ⚠️ 我一度以为合并后仍是"画完存外部→再清→再画→再存"。那是**没合并**的样子。
> 反证就在这张面板上：只有一组 Load/Store；真按那样走应该看到两组。
> 直觉检验：中途若真清了 tile memory，Skybox 会把 Opaque 物体全盖掉，画面里方块早没了。

**③ 合并为什么会断**（同一处的 Pass break reasoning）：

> Failed to merge Copy Depth into this native pass.
> **The next pass reads data output by this pass as a regular texture.**

一旦下一个 pass 把输出当**普通纹理**读（而不是继续当 attachment 用），数据必须落显存 → 合并链断开。

**④ 我的 Blur 为什么出局**（Pass List → Vertical / Horizontal Blur）：

> This is an **Unsafe Render Pass**. Only **Raster Render Passes** can be merged.

完整因果链：

```
AddBlitPass → Unsafe Render Pass → 没有 attachment（面板显示 No attachments）
                                        ↓
                                  谈不上 attachment 一致
                                        ↓
                                     不能被合并
                                        ↓
                          中间结果必须落显存（Memoryless: False）
                                        ↓
                              多一次 store + 一次 load
```

**结论**：机制④ **在这条管线里生效**（Opaque + Skybox 合并了），
但**没作用在我的 Blur 上**，原因是 pass 类型（Unsafe ≠ Raster）。

---

### 1.7 附：`(RP x:y)` 记号彻底解开（补 D2 第 9 题）

```
(RP 1 : 0)
     ↑   ↑
     │   └─ Subpass #  ← Viewer 的 Render Graph Pass Info 里那个 "Subpass #0"
     └───── Pass List 的第几行（第几个 native render pass）
```

Pass 类型也标在名字后面：`Draw Opaque Objects (Raster Render Pass)` / `Vertical Blur Render Pass (Unsafe Render Pass)`

---

### 1.8 附：管线里最大的一块 RT

```
_MainLightShadowmapTexture   2048×2048×1   D16_UNorm   ≈ 8 MB
```

生命周期极短（`Draw Main Light Shadowmap` 写 → `Draw Opaque Objects` 读，之后虚线），却占 8MB。
对比 `_BlurTexture` 才 243×137 —— **大小差 250 倍**。

→ **W5 做真实优化时第一个该看这里**：这 8MB 有没有被别人别名复用？能不能降到 1024²（省一半）？
→ 找性能问题要从大的开始看，这就是 W4 抓帧分析表存在的意义。

---
### 知识扫盲
01.Analysis：分析菜单 不参与游戏逻辑专为性能排查

02.Render Graph Viewer中的Resource List
| 字段名 | 当前值 | 核心含义 |
| :--- | :--- | :--- |
| Size | 291x164x1 | :--- |
| Format | R8G8B8A8_UNorm | 像素颜色格式与存储范围 |
| Samples | 1 | 抗锯齿采样次数（1 = 未开 MSAA） |
| BindMS | False | 是否作为多采样纹理（Texture2DMS）绑定给 Shader |
| Memoryless | False | 是否属于“不占真实物理显存”的纯片上临时图 |

size + Format就是判断是否可以合并RT的依据 这俩合起来就是属性

**“无物理显存”特性** —— 这是移动端(iOS Metal/Vulkan TBRD架构)省内存的绝技。
- 若为ture：该纹理不占用任何正真的物理显存(0字节VRAM)，只活在手机GPU的高速芯片的片上告诉缓存(Tile Memory)中，用完立刻丢弃，不写回主存(常常用于临时的深度图 G-Buffer)

- 若为false：说明模糊图分配了真实的物理显存地址，可以被外部读写。

UniversalResourceDate：包含URP中使用的所有纹理资源，其中包括相机当前激活的颜色纹理和深度纹理
UniveraslCameraDate:包含了当前激活相机的相关的数据
build：这个变量是IRasterRenderGraphBuilder接口的一个实例。这个变量是配置该render pass相关信息的入口点。

---

## 步骤 2 · RG API 为什么这样设计（读文档）

### ① `RecordRenderGraph` 只是录制，不是执行
- 录制阶段干什么：声明pass依赖资源 ADG的线路
- 执行阶段干什么：依靠声明阶段返回的数据和命令执行
- 两者中间隔着什么：
1.录制阶段 URP调用所有pass的RecordRenderGraph() 往Graph中加节点
                    ↓
2.编译图 + 跑四条优化
                    ↓
3.执行 SetRenderFunc 中的内容

### ② `SetRenderFunc` 为什么强制 static lambda + PassData
- 如果用普通 lambda 捕获外部变量，会出什么问题：
理由1：(主要)录制和执行分开录制时闭包获取的引用 到执行的那一刻可能已经变了或者没了
理由2：出现闭包现象 如果每秒60帧就会每秒new60个对象产生严重的垃圾占用 所以才使用static lambda
- PassData 的作用：给graph这次pass使用到的数据

不理解: (static 强制你不能捕获任何外部变量 → 只能把数据装进 PassData → 而 PassData 的生命周期是 RenderGraph 自己管的，保证执行时还活着。)不明白为什么PassData的生命周期时GenderGraph管的层级模糊没有串起来

### ③ `builder.UseTexture` vs `SetRenderAttachment`
不是"输入 vs 输出"，是"怎么用
- `UseTexture` 的语义：当普通纹理采样 数据必须要在显存中 合并断开
- `SetRenderAttachment` 的语义：当渲染目标绑定 可以继续留在Tile Memory合并得以继续
理由：
    1.UseTexture允许shader全屏任意采样，而当前片上缓存(Tile Memory)仅持有局部数据，GPU只能把整张图写回显存才能全屏读取，合并被打断
    2.SetRenderAttachment仅读写当前Tile像素位置自身，无需访问全屏数据，数据直接留在Tile Memory中实现合并。

### ④ `AddUnsafePass` 为什么让管线变慢
- 它绕过了什么：绕过了RemderGraph的资源依赖分析
- 因此哪条机制失效：没有Attachment就谈不上Attachment一致，从类型上就出局了

---

## 步骤 3 · 回到我自己的 BlurRenderPass

对着 `Assets/01/BlurRenderPass.cs` 逐段回答：

> **录制时只有承诺，执行时才有实物。**

| 行 | 代码 | 它在 RG 里扮演什么角色 |
|---|---|---|
| :45 | `RecordRenderGraph(RenderGraph, ContextContainer)` | 注册pass 资源依赖声明 |
| :47-49 | `frameData.Get<UniversalResourceData/CameraData>()` |Get<UniversalResourceData>()获取这帧的全部资源 Get<UniversalCameraData>()获取这帧相机的(矩阵 目标描述 cameraType) |
| :61 | `resourceData.activeColorTexture` | 拿到一张将来会有内容的图的凭证 |
| :62 | `UniversalRenderer.CreateRenderGraphTexture(...)` | 向图登记我需要一张这样的图 |
| :71-72 | 第一次 `AddBlitPass`（垂直） | 录制阶段在graph注册垂直模糊的节点 声明读srcCamColor/shader[0] 执行阶段写入垂直模糊 |
| :75-76 | 第二次 `AddBlitPass`（水平） | 录制阶段在graph注册水平模糊的节点 声明读dst/shader[1] 执行阶段写入水平模糊 |

**为什么要 ping-pong（先 blit 到 `_BlurTexture` 再 blit 回来），不能原地读写？**

>因为会产生读写冲突 同一张资源不能同时又读又写

---

## 步骤 4 · 今日判据作答

**在 Viewer 里指着我的管线，四条机制各是什么状态：**

| 机制 | 生效？ | 证据（在 Viewer 的哪里看到的） |
|---|---|---|
| ① 裁剪 | 看不到 | Draw Transparent Object在图中存在 但未被标记culled。我这条管线没有pass符合“输出没人读条件”的条件 |
| ② 生命周期 | 生效 | _BlurTexture行：Vetical Blur首次写(红) Horizontal末次读(绿)，横跨两个pass，两头虚线 |
| ③ 别名 | 看不出 | Viewer无此视图 |
| ④ 合并 | 生效 | Natice Pass was created by merging 2 Raster Render Passes；整组只有一套Clear/Store。我的Blur时Unasfe Render Pass，only Raster Render Passes can be merged |

---

## 欠账

-
