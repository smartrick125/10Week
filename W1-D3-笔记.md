# Week 1 · Day 3 笔记（2026-09-21 · L3 上 RenderGraph 四条机制）

**今日判据**：**不看笔记**复述 裁剪 / 生命周期 / 别名 / 合并 各解决什么问题。
**主教材**：URP 手册 Render graph system 章节（只读子页 1「Introduction」和 9「Optimize a render graph」）
**工程**：`F:\unity\TestProject`

---

## 步骤 0 · D2 复习自测（2026-09-21 第一次）

> **成绩：4 全对 / 1 基本对 / 3 半对 / 2 错。** D6 盖住"标准答案"列再测一次。

| # | 题 | 我当时答的 | 标准答案 | 判定 |
|---|---|---|---|---|
| 1 | `RenderPassEvent` 代表内容还是时刻？ | 代表时刻不代表内容，我画后处理却用 `AfterRenderingSkybox` | **时刻**。任何 pass 都能挂任何时刻 | ✅ |
| 2 | 数值为什么留空档？怎么用？ | 留给 feature 插队，`AfterRenderingSkybox + 1` | 底层是 int，`RenderPassEvent.AfterRenderingSkybox + 1` 精确插到某格之后、下一格之前 | ✅ |
| 3 | `Create()` 多久调一次？ | play 模式初始化一次、改 shader 一次 | 只在**序列化时刻**：`OnEnable`(:77) / `OnValidate`(:86) / **Renderer 构造函数**。与帧无关 | ⚠️ 漏第三种 |
| 4 | `AddRenderPasses()` 多久调一次？ | 每个相机 × 每帧 | **每帧 × 每个正在渲染的相机**，各一次 | ✅ |
| 5 | 为什么 `new Material` 必须在 `Create()` 里？ | 只需要一个材质，初始化生成一个就够 | 答的是"为什么可以"。**判据问的是代价**：放进 `AddRenderPasses()` = 60fps 双相机每秒 new 出 **120 个没人回收的 Material** | ⚠️ 缺代价 |
| 6 | `SortStable` 为什么稳定？机制在哪？ | 知道结论，不知道具体判断逻辑 | `ScriptableRenderer.cs:2382` 的 `curr < list[j]` —— **是 `<` 不是 `<=`**，事件值相等时不往前挪 | ⚠️ 缺锚点 |
| 7 | `cameraType == CameraType.Game` 拦掉谁？在哪层拦？ | 像白名单，只放行 Game，Scene/材质预览被拦 | 拦掉 SceneView / Preview / Reflection。**Unity 照样每帧调我的 `AddRenderPasses`，是我在方法体内部拦下 `EnqueuePass`** | ✅ |
| 8 | CopyDepth / CopyColor 哪来的？ | URP Asset 里的设置选项（说不出英文名） | **Depth Texture** 和 **Opaque Texture** 两个勾，生成 `_CameraDepthTexture` / `_CameraOpaqueTexture` | ⚠️ 缺英文名 |
| 9 | `(RP x:y)` 是什么？ | x 是整张画纸，y 是作画的不同步骤 | **x = 第几个 native render pass，y = 第几个 subpass。x 相同 = 被合并** | ❌ |
| 10 | 为什么我的 Blur 没有 RP 前缀？ | 因为 feature 能插到任意位置，顺序不固定 | **没被合并**，原因是 **pass 类型**：`AddBlitPass` 产生的是 **Unsafe Render Pass**，Viewer 原话 `Only Raster Render Passes can be merged`。Unsafe pass 连 attachment 都没有。内置 pass 位置也固定，照样带前缀。**（D4 更正）** | ❌ |

**这次暴露的薄弱区**：第 9、10 题（native render pass 与合并）—— 正好是 D3 第④条机制，今天已补。

## 步骤 1 · 旧写法痛点（为什么需要 RenderGraph）

> 左列摘自我自己的计划文档 `10周渲染专精计划.md` 的 L3 对照表。
> **注意：痛点 5 条，机制只有 4 条 —— 第 1 行是地基，不是优化。**

| # | 旧 Compatibility Mode 的痛点 | RenderGraph 怎么解决 | 四条之一？ | 具体表现（我自己填） |
|---|---|---|---|---|
| 1 | 每个 pass 自己 `GetTemporaryRT` + `SetRenderTarget`，互不知情 | Record 阶段声明读写，**构出一张 DAG** | ❌ **地基** | Render Graph系统 当使用Render Graph API创建一个Scriptable Render Pass时 要告诉URP两件事前 1.声明/录制阶段 使用到哪些纹理或者渲染纹理 2.执行阶段 执行哪些图形命令 这些命令使用录制阶段哪些纹理或者渲染纹理 写好之后就可以把这个Scriptable render pass注入到URP渲染器中，我们写的就会成为Render Graph的一部分 DAG的价值是让URP知道pass之间谁依赖谁 后面四条优化全部建立在这一点上：<br>1.知道依赖关系->发现没人读我的输出->裁剪<br>2.知道依赖关系->算出“首次写 到 末次读”->声命周期<br>3.知道生命周期->判断“两张RT是不重叠”->别名<br>4.知道attachment一致->才敢合并->合并|
| 2 | 输出没人用也照样执行 | **pass 裁剪** | ✅ ① | 如果某个pass的输出没有被用到这一帧就把它移除 |
| 3 | RT 生命周期靠人肉管理，容易泄漏 | **生命周期分析** | ✅ ② | RenderGraph系统只在第一个 写入 该资源的render pass之前才为它分配内存，并在最后一个 读取 他的render pass之后释放内存。这仍RenderGraph能以尽可能高效的方式管理内存，并避免为本帧用不到的资源分配内存。 |
| 4 | 每个 RT 独占一块显存 | **内存别名** | ✅ ③ | 当一张纹理与某张更早的纹理具有相同的属性时，复用已分配的内存 |
| 5 | 移动端 TBDR 反复 load/store 带宽爆炸 | **Native RenderPass 合并** | ✅ ④ | 在使用基于图块的延迟渲染(TBDR)的移动平台上，RenderGraph系统还可以把多个render pass合并成单个原生render pass(native render pass)。native render pass会把纹理保留在图块内存(tile memory)，避免将数据从GPU芯片内部的Tile Memory写入外部显存/系统内存。这样一来，URP使用内存带宽和渲染时间都会更少 |
### 概念扫盲
01.区分RT 渲染资源/目标 vs RTHandle 指向并管理 RT的C#句柄 vs Attachment 是RT在某个Render Pass中承担的角色

    1.RT(RenderTarget / RenderTexture) 
        是什么 Render Target GPU当前要把结果画到哪里 是一种用途
                RenderTexture Unity中可以充当Render Target的一种纹理资源
        总结来说：一个GPU可以访问的图像资源，可以被绑定为渲染输出目标
    2.RTHandle / TextureHandle
        是什么 CPU/C#侧管理的对象，真正的大量像素数据不存放在RTHandle对象那里
        总结来说：带有尺寸管理功能的智能 引用/句柄
    3.Attachment 与他们是什么关系 是“某个 Pass 把这张纹理接到哪个插槽”的绑定关系
        兼容模式可以把RTHandle设置为渲染目标：
            RTHandle cameraColor;
            configureTargent(cameraColor);
        此时：
            cameraColor:RTHandle
            底层资源:RenderTexture
            当前角色:Color Attachment
最终关系：
Compatibility Mode：
RTHandle → ConfigureTarget() → 成为 Attachment

Render Graph：
TextureHandle → SetRenderAttachment() → 成为 Attachment

Attachment = Pass、纹理资源、输出槽三者之间的绑定关系

02.tile memory：内存图块， 在GPU内部 移动GPU片上的高速内存， 避免与显存往返

03.Compatiblity Mode:兼容模式 让URP暂时使用旧版渲染路径

04.GetTemporaryRT：申请临时画布

05.setRenderTarget：把这张画布设置为接下来的绘制目标

06.DAG；(Directed Acyclic Graph由 RenderGraph 引擎每帧动态构建) 有向；有明确的连线方向 无环； 不能绕箭头回到原点 图；由节点和连线组成 节点：Render Pass 箭头：Pass之间的资源依赖 方向；数据从哪个pass流向哪个pass 

07.TBDR：(Tile-Based Deffrred Rendering)是移动端的GPU硬件工作方式 先把屏幕分为小块然后决定每个图块需要绘制什么Deferred指延后处理图块并不代表使用G-Buffer
08.Deferred Shading：传统延迟渲染 写入多张G-Buffer再统一写入计算光照 因为要频繁写入多张纹理所以移动端宽带压力大 但是不是完全不支持

09.Attachment:通常指被绑定为颜色或深度渲染目标的纹理，而不是一种独立的类型。
是 Render Pass 当前接上的“画布和深度板”；普通 Texture 是 Shader 拿来参考的“素材。
举例：
RenderTexture / TextureHandle = 一张画布
Attachment = 把这张画布街道渲染输出槽位上

10.Load：开始时要不要旧内容
    load 保留并载入Attachment原来的像素
    clear 不是用就内容先清理成指定颜色或深度
    Dontcare 不需要旧内容也不清除(旧内容丢弃->内容暂时未定义)
11.Store；结束时要不要新结果
    Store 把Tile Memory中的新结果保存到外部内存
    DontCare pass结束后绘制结果直接丢弃

12.RT的位置：RT 是 GPU 图形资源；其像素通常位于 VRAM 或共享系统内存，TBDR 绘制期间还可能暂存在 Tile Memory。RTHandle 和 TextureHandle 只是 CPU 侧引用它的句柄。
**答"RenderGraph 有什么用"的标准结构 = 1 地基 + 4 优化**：
先说地基（声明读写、构出依赖图），再说四条优化。少了地基，四条就成了背书。

## 步骤 2 · 四条机制：各解决什么

### ① Pass 裁剪（culling）
- **解决的问题**：pass 的输出没人读，却照样执行，白花 GPU 时间
- **怎么做到的**：靠 DAG 反向追溯，从最终输出往回走，走不到的 pass 整条剔掉
- **在我的管线里的证据**：**D4 已查 → 看不到**。`Draw Transparent Objects` 在 Viewer 里存在、未被标 culled；这条管线没有 pass 符合「输出没人读」的条件

### ② 生命周期分析（resource lifetime）
- **解决的问题**：人肉管理RT容易疏忽内存泄露
- **怎么做到的**：每个RT 在第一个写入它的pass之前创建内存 在最后一个读取它的pass之后释放内存
- **在我的管线里的证据**：**D4 已查 → 生效**。`_BlurTexture` 行：`Vertical Blur` 首次写（红）→ `Horizontal Blur` 末次读（绿），横跨 2 个 pass，两头虚线

### ③ 内存别名（memory aliasing）省的是显存容量
- **解决的问题**：每张 RT 各占一块显存，峰值占用高
- **怎么做到的**：靠 DAG 算出的生命周期，判断两张 RT 是否**属性相同 + 生命周期不重叠**。两个条件缺一不可 —— 属性相同才装得下，生命周期不重叠才不会互相覆盖
- **在我的管线里的证据**：**D4 已查 → 看不到**。Viewer 没有别名视图，View Options 只有 `Load Store Actions`

### ④ Native RenderPass 合并（pass merging）省的是带宽（load/store 次数）
- **解决的问题**：避免重复对Tile Memory进行load和store操作这样浪费带宽
- **怎么做到的**：当两个或者多个pass的Attachment相同时就先将前一个的结果保留在Tile Memory中下一个直接使用
- **在我的管线里的证据**：**D4 已查 → 生效，但我的 Blur 出局**。Viewer 原话 `Native Pass was created by merging 2 Raster Render Passes`，整组只有一套 Clear/Store；我的 Blur 是 Unsafe Pass

---

## 步骤 3 · 回答 D2 留下的钩子

**钩子③**：场景里没有透明物体时，透明那一格在 Frame Debugger 里**整格消失**，而不是显示一个空 pass。
是 RenderGraph 裁剪掉了（机制①），还是它执行了但没有 draw call？

>没有draw call因为在Render Graph Viewer中显示了说明pass生效了只不过没有画而已

**钩子①**：`BlurRenderPass.cs:71-76` 一个 `ScriptableRenderPass` 里加了两个 blit pass。
在 `m_ActiveRenderPassQueue` 里算几格？在 RenderGraph 里算几个？

>**队列里 1 格**（`BlurRendererFeature.cs:32` 只调了一次 `EnqueuePass`，塞进去的是一个 `ScriptableRenderPass` 对象）；**RenderGraph 里 2 个节点**（`:72` 和 `:76` 两次 `AddBlitPass`）。1→2 的展开发生在我自己的 `RecordRenderGraph` 方法体内。
>
>另有 1→0 的情况：`BlurRenderPass.cs:53` 命中 `isActiveTargetBackBuffer` 提前 return 时，队列那格照样在，图里 0 个节点。

>裁剪的是图节点，不是队列中的pass对象。blur就算被裁光了两个节点它在 m_ActiveRenderPassQueue 里那一格照样在。

**钩子②**：Frame Debugger 的 RP 编号 `0 / 1 / 1 / 2 / 3 / 5` 跳过了 4，为什么？

>没有画RP为4的物体 因为Render Graph Viewer中有透明物体的记录但是Frame Debugger中没有就只能说明没有透明物体被画出所以就不显示 但是不代表没有这个pass

---

## 步骤 4 · 今日判据作答（合上笔记和文档写）

**裁剪解决什么**：

>解决了已经执行输出的pass没有人读取 白白花费GPU时间

**生命周期分析解决什么**：

>解决RT内存分配不精确，人肉管理容易泄露；靠DAG算出每张RT从 首次写 到 末次读 的区间

**内存别名解决什么**：

>解决显存峰值占用的问题 依赖pass生命周期 判断不同pass的RT是否可以使用同一块显存

**合并解决什么**：

>解决带宽的问题 依赖Attachment是否相同的判断（前提：双方都得是 Raster Pass）将可兼容的 pass 合成一个 native pass，中间结果留在 Tile Memory 不落显存，减少 load / store 操作

---

## 欠账

-
