# Week 1 · Day 1 笔记（2026-09-18 · L1 SRP 执行流程）

**今日判据**：能说清 `Submit()` 为什么必须在最后。→ **✅ 通过**
**实际投入**：约 8h（预算 5.5h，超 2.5h）
**工程**：`F:\unity\Week10`（Catlike Custom SRP 练习工程，勿与 `F:\unity\TestProject` 混）

---

## 1. 主干时序（自己这条 CameraRenderer 的实际调用链）

```
CustomRenderPipeline.Render(context, List<Camera>)
  └─ 逐相机 → CameraRenderer.Render(context, camera)
       1. Cull()                    ← 失败直接 return，此时一点 GPU 活都没干
            camera.TryGetCullingParameters(out p)
            cullingResults = context.Cull(ref p)
       2. Setup()
            context.SetupCameraProperties(camera)
            buffer.ClearRenderTarget(true, true, Color.clear)
            buffer.BeginSample(bufferName)
            ExecuteBuffer()         ← ExecuteCommandBuffer + buffer.Clear()
       3. DrawVisibleGeometry()
            context.DrawSkybox(camera)
            context.DrawRenderers(cullingResults, ref drawing, ref filtering)
       4. Submit()
            buffer.EndSample(bufferName)
            ExecuteBuffer()
            context.Submit()        ← 到这一行命令才真正送到 GPU
```

> **URP 自己那条时序还没读（步骤 3 跳过），记为债务，D2 一并补。**

---

## 2. 四个知识点

### ① Pipeline Asset 和 Instance 的关系
- Asset = 硬盘上的 ScriptableObject，一张配置表，职责只有一个：`CreatePipeline()`。
- Instance = 真正干活的工人，每帧被调用 `Render()`。
- **实例跨帧存活，不是每帧新建**；只有 Asset 被修改/重新赋值时才 Dispose 并重建。
- 实验验证：在 `CustomRenderPipeline` 构造函数打日志 → 满场跑相机**不打印**，改一下 Asset 再存**才打印**。

### ② Command Buffer：为什么"先录制后提交"
- `context.ExecuteCommandBuffer(buffer)` 只是把命令**拷贝**给 context，buffer 自己那份列表不会清空 → 所以必须紧跟 `buffer.Clear()`。
- 实验验证：注释掉 `buffer.Clear()` → 屏幕变黑，Frame Debugger 条数每帧疯涨（命令在累积叠加）。

### ③ Culling 在整帧里的位置
- **cull 在 clear 之前**（`Render()` 里 `Cull()` 先于 `Setup()`）。
- 两个理由：
  1. `cullingResults` 是 `DrawRenderers` 的输入，必须先有。
  2. cull 失败要**早退**，早退时连屏幕都不该清 —— GPU 一点活都不该干。
- `TryGetCullingParameters` 返回 false 时，`ScriptableCullingParameters` 是 **struct 不是 null**，拿到的是全零默认值，硬送进 `Cull()` 会报错。

### ④ 三个 Settings 各管什么
- **SortingSettings**：以相机为基准点给物体排序。
- **FilteringSettings**：选哪一批物体渲染（如 `RenderQueueRange.opaque` = 只画不透明）。
- **DrawingSettings**：与 shader 的 `LightMode` 标签关联，控制走哪个 pass；也能再注册额外 tag，让同一批里不同 LightMode 的物体一起画。

---

## 3. 今日判据作答

**`Submit()` 为什么必须在最后？**

> 整条管线里的命令都是**攒着的**，不是写一句执行一句。`ExecuteCommandBuffer` 只是把命令交给 context 排队，`context.Submit()` 才是"提交"那一下，命令这时才真正送到 GPU。所以注释掉 `Submit()` 画面直接全黑 —— 一条命令都没送出去。放在最后是因为这一帧要画的东西必须全部录完才提交。

---

## 4. 证据记录（Frame Debugger）

实际列表顺序：`Render Camera` → `Clear (color+depth+stencil)` → `Camera.RenderSkybox` → `Draw Mesh` / `RenderLoop.Draw` → `Draw Mesh Cube ×2`

- 场景放 3 个 cube，**1 个故意放在画面外做对照** → 它没出现在 Frame Debugger 里 = **culling 生效的直接证据**。

---

## 5. 欠账（留给 D6，或 D2 顺手带过）

1. ~~**URP 源码 ①②③ 没读**~~ → **D2/D3 已补 ②③**：每帧入口 `UniversalRenderPipeline.cs:442`、Submit 在 `:921`、culling 在 `:850`（在 `RecordRenderGraph` 之前）。**只剩 ①「谁创建管线实例」未读**（`Data/UniversalRenderPipelineAsset.cs:811 CreatePipeline()`），D6 补。
2. **天空盒顺序要改**：当前代码 `DrawSkybox` 在 `DrawRenderers` **之前**（`CameraRenderer.cs:57`），教程是之后。
   结论已想通：**应该先画物体占住深度，再画天空盒**，否则天空盒被整块覆盖 = 白白浪费填充率（overdraw）。
   → 待办：把 57 行挪到 `DrawRenderers` 之后。
3. `CameraRenderer.cs:74` 拼错的空方法 `DrawVisiblaGeometry()` 待删。
4. **SortingSettings 实验没做**：51 行 `new SortingSettings(camera)` 没显式设 `criteria`。
   待办：设成 `SortingCriteria.CommonOpaque` 再看 Frame Debugger 绘制顺序是否翻成"近→远"。
   URP 实锤：URP 构造 `SortingSettings` 的四处**全部显式写 criteria**，从不用默认值（`RenderingUtils.cs:304`）；
   不透明 / 透明两套排序见 `Passes/DrawObjectsPass.cs:225`（D2 要读的文件）。
5. **透明 cube 画出来但看不见**：built-in shader 的 alpha/贴图默认值问题，**与管线无关**。
   按铁律③（不能回答本周验收问题就不学）→ 收藏，不追。

---

## 6. 追加验证：三道关

物体要被画出来得连过三关，`RenderQueueRange.all` 只管第三关：

1. **Culling** —— 不在视锥内直接没了（画外那个 cube 就是证据）
2. **ShaderTagId 匹配** —— 只收 `SRPDefaultUnlit`，匹配不上的 shader 默认什么都不画（Catlike "Unsupported Shaders" 那节处理的就是这个）
3. **FilteringSettings 队列范围** —— `all` / `opaque` 管的是这一关

**FilteringSettings 已实验验证** ✅：把一个 cube 放进 Transparent 队列后，
`RenderQueueRange.all` → Frame Debugger 里有它的 draw call；
`RenderQueueRange.opaque` → draw call 消失。
→ 证明 FilteringSettings 是**白名单**（只保留这个范围），不是黑名单。

> 教训：换 shader 同时改了"队列"和"pass 标签"两个变量，不干净。
> 单变量做法是材质仍用 Unlit/Color，只在 Inspector 里改 Render Queue。
