# Week 1 · Day 5 笔记（2026-09-23 · L4 落到自己的管线）

**本周验收物**：① 完整时序图 ② 每个 pass 的输入输出 RT 表
**今日判据**：**时序图能讲给别人听**

> ### 工程选择记录（2026-09-23）
>
> 计划原文的 L4 表要求填 **Sobel + 高斯模糊**，但这两个**都不在 TestProject**，
> 而在 **UnityShaderBook**。计划里「主工程是 TestProject」那句需要修正。
>
> **决定：D5 时序图仍画 TestProject。** 理由 —— D2~D4 三天的证据（Viewer 截图 /
> Frame Debugger / 源码行号对照）全是它的；且它只有 1 个 active feature，第一次画图更容易讲通。
>
> **UnityShaderBook 的坐标（W2 手推高斯核 / W8 像素描边时直接用）：**
>
> 根目录 `F:/unity/UnityShaderBook`，效果都在
> `Assets/Scripts/UnityShaderBook/Chapter12/` 下：
>
> | 文件夹 | 内容 |
> |---|---|
> | `EdgeDetction/` | `EdgeDetection.shader`（`:24 half Sobel`）/ `_RenderPass.cs`（`:53 RecordRenderGraph`）/ `_RendererFuture.cs` / `_CustomVolumeComponent.cs` |
> | `GaussianBlur/` | `GaussianBlur.shader` / `_RenderPass.cs` / `_RenderPas1.cs` / `_RendererFeature.cs` |
> | `Bloom/` `MotionBlur/` `Exmaple-Post-processing/` | W2 之后可能用到 |
>
> 该工程 `Assets/Settings/PC_Renderer.asset` 挂了 **8 个 feature**（括号内 = `m_Active`）：
> 亮度饱和度对比度(0) / 高斯模糊(0) / ScreenSpaceShadows(**1**) / MotionBlur(**1**) /
> 模糊效果(0) / 辉光(0) / SSAO(**1**) / 边缘检测(0)
>
> ⚠️ 它是 URP 工程、同样 6000.3.9f1，**Sobel 已经是 RenderGraph 写法**，W8 不用迁移。

**工程**：`F:\unity\TestProject`

---

## 步骤 0 · 还欠了四天的源码表⑦（45 分钟）

`Runtime/Passes/DrawObjectsPass.cs` —— D1 学的三个 Settings，URP 实际填了什么。

| 问题 | 位置 | 答案 |
|---|---|---|
| 收哪些 shader tag（LightMode） | `:17` `m_ShaderTagIdList` + `Init()` `:88-94` | SRPDefaultUnlit UniversalForward UniversalForwardOnly |
| 不透明用什么排序 | `:225` `cameraData.defaultOpaqueSortFlags` | 在不支持HSR情况下 按照传统方法从前到后 支持的情况下随意画 证据UniversalRenderPipeline.cs:1572 |
| 透明用什么排序 | `:225` `SortingCriteria.CommonTransparent` | 这个因该是半透明吧？从远到近画 但是我没找到证据 |
| 2D 情况下的排序 | `:227` | 先按照Layer排序 然后是Queue 然后是优化部分合批优化相同的材质尽量一起画 然后就是UI排布 |
| `perObjectData` 开了哪些位 | 搜 `perObjectData` | 不在此文件 |

**和 D1 的对照**（我自己写的 `CameraRenderer.DrawVisibleGeometry` vs URP）：

>

### 知识扫盲
01.HSR: (Hidden Surface Removal 隐藏面消除)当检测到当前的GPU硬件支持HSR时URP会利用该特性不再从前往后排序
        传统机制的矛盾: 以前从前往后画会出现 木箱->铁桶->木箱的情况导致同一种材质被打散，
    合批被拆碎，Draw Call暴增。
        手机端( TBDR/ Aapple / PowerVR )的硬件HSR:现代移动芯片在执行片元着色器的之前，
    硬件架构层会自动把整块Tile内被挡住的像素在纳秒级直接剔除，意味着就算从后往前乱序画也不会多一个废像素。
02.useDepthPriming(深度预通过 / pre-Z): 画物体颜色前，管线先专门跑一个深度缓冲(Z-Buffer)预先刷好了 真正画颜色的时候深度测试设置为Equal(只画刚好贴在最表面的像素)   
03.
    Sortingcrieria: 总作用(GPU进场的交规)它是Unity渲染管线里的排队总交规 他告诉GPU: 先
        按图层排队，再按队列排队， 中间尽量把相同的材质一起画
    SortingLayer：(排序层)Unity编辑器中支持自定义Sorting Layer排序时先看图层位于
        Backgtound的必须先画 位于Player的后画
    RenderQueue: (渲染队列)
    OptimizeStateChanges: (状态优化 / 合批神器)它告诉排序器在满足上面的条件下，把使用同
        一个材质，同一套贴图的物体四四捆绑在一起画，极大减少了显卡的切换状态的开销(消灭SetPass Call)
    CanvasOrder: (UI画布次序)针对场景中的World Space UI(世界3DUI画布)；按照Canvas组件
        上的Order in Layer数值排队保证UI覆盖正确
---

## 步骤 1 · Pass 表（本周验收物之一）

数据来源：Render Graph Viewer（Main Camera）。
⚠️ 计划原文要求填 Sobel，但**这个工程里没有 Sobel**，用实际存在的 pass 代替。

| # | pass | 输入 RT（格式 / 分辨率） | 输出 RT（格式 / 分辨率） | 位置 | 备注 |
|---|---|---|---|---|---|
| 1 | Draw Main Light Shadowmap | null | _MainLightShadowmapTexture(D16_UNorm 2048x2048x1) | UniversalRendererRenderGraph.OnBeforeRendering:750 | 与Draw Additional Light Shadowmap, Setup Camera Properties合并为一个native pass; 整条管线中唯一[只写不读]的pass 数据流的源头 |
| 2 | Draw Opaque Objects | DefaultShadowTexture(D32_SFloat 1x1x1), _CameraDepthAttachment(D32_SFloat_S8_UInt), _MainLightShadowmapTexture(D16_UNorm 2048x2048x1) | _CameraTargetAttachment(B10G11R11_UFloatPack32), _CameraDepthAttachment(D32_SFloat_S8_UInt 200x113x1) | UniversalRendererRenderGraph.OnMainRendering:1235 | 与 3 合并成一个 native pass |
| 3 | Draw Skybox | null | _CameraTargetAttachment(B10G11R11_UFloatPack32), _CameraDepthAttachment(D32_SFloat_S8_UInt 200x113x1) | UniversalRendererRenderGraph.OnMainRendering:1269 | 与 2 合并 |
| 4 | Copy Depth | _CameraDepthAttachment(D32_SFloat_S8_UInt 200x113x1) | _CameraDepthTexture(R32_SFloat 200x113x1) | UniversalRendererRenderGraph.CopyDepthToDepthTexture:976 | 由 URP Asset 的 Depth Texture 开关决定 |
| 5 | **Vertical Blur Render Pass** | _CameraTargetAttachment(B10G11R11_UFloatPack32 200x113x1) | _BlurTexture(R8G8B8A8_UNorm 200x113x1) | RenderPassEvent:400 | shader pass 0 = BlurPassVertical |
| 6 | **Horizontal Blur Render Pass** | _BlurTexture(R8G8B8A8_UNorm 200x113x1) | _CameraTargetAttachment(B10G11R11_UFloatPack32 200x113x1) | RenderPassEvent:400 | shader pass 1 = BlurPassHorizontal |
| 7 | Downsample Color (CopyColor) | _CameraTargetAttachment(B10G11R11_UFloatPack32 200x113x1) | _CameraOpaqueTexture(B10G11R11_UFloatPack32 100x56x1) | UniversalRendererRenderGraph.OnMainRendering:1283 | 由 Opaque Texture 开关决定 |
| 8 | Draw Transparent Objects | DefaultShadowTexture(D32_SFloat 1x1x1), _CameraDepthAttachment(D32_SFloat_S8_UInt 200x113x1), _CameraDepthTexture(D32_SFloat_S8_UInt ), _MainLightShadowmapTexture(D16_UNorm 2048x2048x1), _CameraOpaqueTexture(B10G11R11_UFloatPack32 100x56x1) |  _CameraTargetAttachment(B10G11R11_UFloatPack32 200x113x1), _CameraDepthAttachment(D32_SFloat_S8_UInt 200x113x1) | UniversalRendererRenderGraph.OnMainRendering:1303 | 场景无透明物体 → 无 draw call |
| 9 | Blit Final To Back Buffer |  _CameraTargetAttachment(B10G11R11_UFloatPack32 200x113x1), _CameraDepthTexture(D32_SFloat_S8_UInt ) | Backbuffer color(B10G11R11_UFloatPack32 189x107x1) | UniversalRendererRenderGraph.OnAfterRendering:1536 (m_FinalBlitPass.Render) | 与Draw UIToolkit/u GUI Overlay合并为一个native pass |

> **填法**：格式和分辨率**跟着每个 RT 写**，不单独占列 —— 它们是 RT 的属性，不是 pass 的属性。
> 一个 pass 读多张、写多张很正常，每张各带自己的格式。
> 例：`_CameraTargetAttachment (R8G8B8A8_UNorm, 243×137)` + `_CameraDepthAttachment (D32…, 243×137)`
>
> **内置 pass** 填「阶段函数 + 源码行」；**自定义 pass** 填「RenderPassEvent 数值 + 注入点行」。
> 两者机制不同：内置 pass 的顺序是**写死在代码里**的，自定义 pass 的顺序是 `SortStable` **按数值排**出来的。

### 知识扫盲
D32_SFloat_S8_UInt（联合深度模板）：
D32_SFloat：32 位单精度浮点深度，彻底消灭远景深度冲突（Z-Fighting）；
S8_UInt：8 位（0~255）无符号整数模板，专用于传送门、镜子与描边遮罩标记。

D16_UNorm（阴影深度图）：
D16：16 位深度，相比 32 位直接省去 50% 的显存与带宽，专为 Sunlight 阴影而生；
UNorm：无符号归一化（Normalized），把底层整数按比例压缩为 0.0 ~ 1.0 距离数值，与“法线”毫无关系。

B10G11R11_UFloatPack32（性价比之王 HDR 颜色图）：
BGR：蓝 10 位、绿 11 位、红 11 位（人眼对绿最敏感，对蓝最迟钝）；
UFloat：支持突破 1.0 的正浮点颜色，承载 HDR 泛光与真实高光；
Pack32：把三色像压缩饼干一样打包挤压进 32 位（4 字节）标准内存单元，用普通 SDR 体积享受顶级 HDR 画面。
### 三道必答题（计划原文要求）

**Q1** 为什么需要 ping-pong 而不能原地读写？

>防止出现读写冲突 同一张RT又读又写的情况

**Q2** 为什么 Blur 放在 Skybox 之后、Transparent 之前？（挂 400 的代价是什么）

>因为需要模糊不透明物体和skybox 但是不需要模糊透明物体 代价就是半透明物体拿到的是模糊后的画面

**Q3** Blur 在 color / depth / normal 哪张图上做？怎么证明？

>在color上做的 Render Graph Viewer中只有_CameraTargetAttachment和_BlurTexture在做读写操作
代码层：BlurRenderPass.cs:61 resourceData.activeColorTexture —— 变量名就写着 Color
格式层：_CameraTargetAttachment 是 B10G11R11_UFloatPack32（颜色格式），深度图是 D32_SFloat_S8_UInt、深度纹理是 R32_SFloat —— 格式本身就能排除 depth

### 核对：挂了 3 个 feature，图里只有 1 个

`Assets/Settings/PC_Renderer.asset` 上挂着 BlurRendererFeature / SimpleInvertRenderGraphFeature / ScreenSpaceAmbientOcclusion 三个。
**去 Inspector 看这三个的启用勾，然后解释为什么 Viewer 里只出现 Blur。**
（源码依据：`ScriptableRenderer.cs:1657`）

>因为if (!rendererFeatures[i].isActive){continue;}只有在RenderPipeline中激活才可以使用

---

## 步骤 2 · 时序图（本周验收物之二）✅ 已完成

**产出**：手绘「调度链路图」一张（纸质，见 2026-09-23 照片）。

**图上画进去的：**
- 六层标注（层1 管线 → 层6 执行）
- `Cull → AddRenderPasses → EnqueuePass → SortStable → OnRecordRenderGraph` 主链
- 排序后的 pass 顺序清单，标出 native pass 分组（①②③）
- 四条优化列在层6
- Blur 的 `AddBlitPass ×2` 展开

**没画 RT 流向图** —— 计划原文要求的是「**说明**每个 pass 的输入输出是什么 RT」，
步骤 1 的 9 行表格已经满足，再画一张只是换形式，不加分。**不记为欠账。**

---

## 步骤 3 · 判据：讲一遍 ✅ 主干通过

### 口述记录（2026-09-23，约 7h 投入后）

```
层1  RenderPipeline 一帧调用一次
层2  Renderer 每个相机一帧调用一次（两个相机就调两次）

层2  Cull
层2  AddRenderPasses 遍历 feature
层3    feature 添加自定义 pass → EnqueuePass 加入队列末尾
层2  SortStable 全部排序
       我的 BlurFeature 设的是 RenderPassEvent.AfterRenderingSkybox = 400
       → 插在 CopyDepth 和 DownsampleColor 中间
       → 关掉 Depth/Opaque Texture 两个开关后，本质位置是 Skybox 和 Transparent 中间
层2  OnRecordRenderGraph 遍历排好的 pass
       层4 点名 Draw Main Light Shadowmap    → 层5 加节点
       层4 点名 Draw Additional Light Shadowmap → 层5 加节点
       层4 点名 Setup Camera Properties        → 层5 加节点
       ...
       层4 点名 BlurPass                      → 层5 加节点 ×2（先纵向，后横向）
       ...
层6  全部录完 → 四条优化（裁剪 / 生命周期 / 别名 / 合并）
       Draw Main Light Shadowmap + Draw Additional Light Shadowmap + Setup Camera Properties 合并
       Draw Opaque Objects + Draw Skybox 合并
       Blit Final To Back Buffer + Draw UIToolkit/uGUI Overlay 合并
层6  逐个 native pass 执行
       Draw Main Light Shadowmap 只输出、不输入 → 整条数据流的源头
         输出 _MainLightShadowmapTexture (D16_UNorm 2048×2048×1)
       Draw Opaque Objects
         输入 DefaultShadowTexture / _CameraDepthAttachment / _MainLightShadowmapTexture
         输出 _CameraTargetAttachment / _CameraDepthAttachment
       ...
层6  全部执行完 → CPU 统一 Submit 发给 GPU
```

### 讲得好的三处

1. **区分了「观察位置」和「本质位置」** —— 400 实际夹在 CopyDepth 和 DownsampleColor 中间，
   但那两个邻居由 URP Asset 开关决定；关掉之后本质是 Skybox↔Transparent 之间。D2+D4 的综合。
2. **三组 native pass 合并全部列对**
3. **结尾补了「统一 Submit 发给 GPU」** —— 呼应 D1 的判据

### ❗讲错的地方（D6 早上重讲）

| # | 讲错的 | 正确的 |
|---|---|---|
| 1 | 「Tile memory 只有 16×16 的空间，所以只能打断写回外部显存」 | **这个数字是编的，而且不是原因**。Unity 原话：`This is an Unsafe Render Pass. Only Raster Render Passes can be merged.` —— **是 pass 类型出局，不是 tile 装不下** |
| 2 | 数据流源头说成 `Draw Opaque Objects` | 是 **`Draw Main Light Shadowmap`**（口误，下一句自己讲对了） |
| 3 | 「裁剪没用到，其他优化全部生效」 | D4 结论是 **2 生效 2 看不到**：生命周期 ✅ / 合并 ✅ / **裁剪和别名都是「看不到」** |
| 4 | `_BlurTexture` 生命周期「不赘述了」跳过 | 判据五条之一，必须讲：**Vertical Blur 写 → Horizontal Blur 读，只活 2 个 pass，两头虚线** |
| 5 | 「一个横向模糊 一个纵向模糊」 | 顺序反了：**shader pass 0 = BlurPassVertical 在前**，pass 1 = Horizontal 在后 |

> **第 1 条最要紧。** 这周一直在练「分清我猜和我验证过」，编一个数字出来正是要避免的失败模式 ——
> W10 模拟面试时这种会被追问到底。

---

## 欠账 → 转给 D6（2026-09-24）

### A. 今天留下的（早上第一件事，10 分钟）

1. **重讲两句**：
   - ① 我的 Blur 为什么没被合并（用 Unsafe / Raster 那条，**不要提 tile 尺寸**）
   - ② `_BlurTexture` 的生命周期（从哪来、到哪去、活多久）
2. **口头纠正另外三条**（数据流源头 / 四条机制状态 / 纵横顺序）

### B. 更早的欠账（D1~D4 累积）

3. **D1 源码①**：谁创建管线实例 → `Data/UniversalRenderPipelineAsset.cs:811 CreatePipeline()`
4. **Week10 练习工程三个小改动**：天空盒行下移 / 删拼错的空方法 / SortingSettings 加 criteria（若尚未做）
5. **D2 复习清单第 9、10 题**：D6 盖住答案再自测一次（上次是这两题错的）
6. **D4 步骤 2 的四条 API 修正**（录制≠执行 / static lambda 主因是失效 / UseTexture vs SetRenderAttachment / AddUnsafePass），若还没抄进笔记
7. **`OnRecordRenderGraph`(723/725/730) 和 `OnBeforeRendering`(841/845) 的注入点**没列全

### C. D6 本职

8. 写 **200 字周记**：这周搞懂了什么 / 还有什么没搞懂
