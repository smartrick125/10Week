# 10 周渲染专精计划（总纲）

> 起始日：2026-09-18（Day 1 / Week 1）
> 总预算 ≈ 280h ｜ 每周 5 天推进 + 1 天补欠账 ≈ 28h/周 ≈ 5.5h/天
> 环境：Unity 6.3 LTS (6000.3.9f1) · URP 17.3.0 · 主工程 `F:\unity\TestProject`

## 铁律（优先级高于一切安排）

1. **这 10 周不许让 AI 直接写实现代码。** 只能用 AI 问原理、查文档、验证理解。
2. **只做渲染这一条战线。** 不加新玩法、不改游戏设计。
3. **判断要不要学一个东西，只有一条标准：它能不能直接回答我本周的验收问题。** 能就读，且只读相关那部分；不能就收藏。
4. **验收不达标就不进下一阶段。** 宁可砍阶段三。三个讲得透的模块 > 六个讲不清的模块。
5. **每周末写 200 字周记**：这周搞懂了什么、还有什么没搞懂。

## 10 周总览

| 阶段 | 周 | 主题 | 验收物 |
|---|---|---|---|
| 一 · 反向拆解（85h） | W1 | URP 与 RenderGraph | 自己这条管线的完整时序图 + 每个 pass 的输入输出 RT |
| | W2 | 两个后处理的数学 | 手推高斯核权重；说清 Sobel 在哪个 buffer、漏哪类边 |
| | W3 | GPU 与光栅化补课 | 解释全屏后处理在移动端为何贵、降分辨率为何有效 |
| 二 · 测量与优化（55h） | W4 | 建立测量能力 | 一张完整的抓帧分析表（DC/SetPass/RT 内存/各 pass GPU 耗时） |
| | W5 | 做一次真实优化 | 优化前后数据对比 + 每项改动为什么有效、代价是什么 |
| 三 · 像素渲染管线（85h） | W6 | 像素完美 | 整数缩放 / 点采样 / camera snapping / 消亚像素抖动 |
| | W7 | 调色板与抖动 | 颜色量化、调色板映射、Bayer 有序抖动，做成可配置 pass |
| | W8 | 像素级描边 + 2D 光照 | 把 Sobel 改成像素网格对齐描边 + 2D 法线光照，缝合全栈 |
| 四 · 产出物（55h） | W9 | 文档 / 视频 / 页面 | 4 篇技术文档上 GitHub、2–3 分钟带实时调参的演示视频、smartrick.top 作品集页 |
| | W10 | 求职收口 | TA 简历定稿、九周验收问题整理成自问自答清单、一次模拟面试 |

**阶段三总验收**：一套可开关、可调参的像素风格渲染栈，每个模块有效果对比图 + 性能数据。

---

# Week 1｜URP 与 RenderGraph

**本周目标**：从"我的 feature 能跑"倒推出"URP 整条管线为什么这样跑"。
**本周验收**：画出自己这条管线的完整时序图，并说明每个 pass 的输入输出是什么 RT。

## 知识点四层（下层必须建立在上层之上）

### L1 SRP 执行流程（地基，约 1 天）
- `RenderPipelineAsset` → `RenderPipeline.Render()` → 谁在每帧调用它
- `ScriptableRenderContext` 与 `CommandBuffer` 的关系：为什么是"先录制后提交"（`context.Submit()`）而不是立即执行
- Culling（`ScriptableCullingParameters` / `CullingResults`）在整帧里的位置
- `DrawingSettings` / `FilteringSettings` / `SortingCriteria` 决定"画哪些、按什么顺序画"

### L2 URP 在 SRP 之上加了什么（约 1 天）
- `UniversalRenderer.Setup()` 里内置 pass 的默认顺序：DepthPrepass → Opaque → Skybox → Transparent → PostProcess → FinalBlit
- `RenderPassEvent` 的全部取值，以及 `BeforeRenderingPostProcessing` / `AfterRenderingPostProcessing` 的差别 —— **这就是"我的 feature 在哪被调用"的答案**
- `AddRenderPasses()` 每帧每相机都调一次，`Create()` 只在重载时调；为什么 Sobel/Blur 的 Material 要在 `Create()` 里建

### L3 RenderGraph 解决了什么（本周核心，约 1.5 天）
先搞清旧写法的痛点，再看 RG 怎么解决，否则记不住：

| 旧 Compatibility Mode | RenderGraph |
|---|---|
| 每个 pass 自己 `GetTemporaryRT` + `SetRenderTarget`，互不知情 | Record 阶段声明读写，构出一张 DAG |
| 输出没人用也照样执行 | **pass 裁剪**：没有后继读取的 pass 直接剔掉 |
| RT 生命周期靠人肉管理，容易泄漏 | **生命周期分析**：从首次写到末次读 |
| 每个 RT 独占一块显存 | **内存别名**：生命周期不重叠的 RT 复用同一块物理内存 |
| 移动端 TBDR 反复 load/store 带宽爆炸 | **Native RenderPass** 自动合并相邻 pass |

配套 API 要搞懂"为什么这样设计"：
- `RecordRenderGraph` 只是录制不是执行
- `SetRenderFunc` 为什么强制 static lambda + PassData（闭包捕获的引用在延迟执行时可能已失效）
- `builder.UseTexture` / `SetRenderAttachment` 的语义差别
- `AddUnsafePass` 为什么让管线变慢（URP 无法分析依赖，合并失效）

> 加粗那四条（裁剪 / 生命周期 / 别名 / 合并）就是面试被问"RenderGraph 有什么用"的标准答案。但必须能在 Render Graph Viewer 里指着自己的管线说出哪条生效了、哪条没生效 —— 比如 Sobel 和 Blur 之间是否被合并、为什么。

### L4 落到自己的管线（约 0.5 天，产出验收物）
给 Sobel、Gaussian Blur 逐个填表：

| pass | 输入 RT | 输出 RT | 格式 | 分辨率 | RenderPassEvent | 备注 |
|---|---|---|---|---|---|---|
| Sobel | | | | | | 在 color / depth / normal 哪张上做？ |
| Blur H | | | | | `AfterRenderingSkybox` | 为什么需要 ping-pong 而不能原地读写 |
| Blur V | | | | | `AfterRenderingSkybox` | 为什么 Blur 放在 Skybox 之后、Transparent 之前 |

## 学习资源（只用这些，不扩张）

1. **URP 手册 Render Graph 章节** —— 本周主教材。docs.unity3d.com 搜 "render graph system in URP"，把整个子目录顺着读完。
2. **URP Package Samples** —— Package Manager → Universal RP → Samples，导入后有一组 RenderGraph 示例工程，对照文档读比看片段快。
3. **Render Graph Viewer**（Window → Analysis → Render Graph Viewer）—— 本周验收直接靠它。它画出所有 pass、每个资源的读写区间和别名情况；时序图 = 看懂它后用自己的话重画一遍。配合 Frame Debugger 看真实合并结果。
4. **源码** —— `Library/PackageCache/com.unity.render-pipelines.universal@<版本>/Runtime/`。不求全懂，只求验证文档说法。
5. **Catlike Coding · Custom SRP** —— 补 L1 最扎实的路径，**只做到"绘制不透明物体"那几章**，不做完。

**明确不学**：Catlike 的 Basics（Jobs/Burst/Compute）、Pseudorandom Noise、Procedural Meshes、Rendering 系列。那是写作顺序依赖，不是知识依赖，20+ 篇要吃掉全部预算的三分之一。

## 关于"读源码"的正确预期

读不到的：`ScriptableRenderContext.Submit()`、`context.Cull()`、`CommandBuffer.DrawRenderer()` 的内部实现 —— C# 层只是壳，真身在不开源的 C++ 里。

**源码解决的不是"这个方法怎么实现"，而是"URP 是怎么用这些方法的"**：
- 这些 API 被调用的先后顺序（culling 在哪、setup 在哪、submit 在哪）—— API 文档不会告诉你
- 参数实际被填了什么（`CreateDrawingSettings()` 默认塞了哪些 shader tag、`perObjectData` 开了哪些位）—— 这决定你的 shader 能拿到什么数据
- **你的 feature 被插在了哪一格** —— `RenderPassEvent` 在源码里对应执行队列的什么位置、前一个 pass 刚写完什么 RT。**这是第 1 周真正的痛点来源，只有源码能回答。**

---

# Week 1 逐日拆解

| Day | 日期 | 对应层 | 主任务 | 当日判据 |
|---|---|---|---|---|
| D1 | 09-18 | L1 | SRP 执行流程：Catlike Ch.1 + 源码主干时序 | 说清 `Submit()` 为什么必须在最后 |
| D2 | 09-19 | L2 | URP 加了什么：内置 pass 顺序 + RenderPassEvent | 指出自己 feature 插在队列的第几格、前后是谁 |
| D3 | 09-20 | L3 上 | 旧写法痛点 → RG 四条机制 | 不看笔记复述裁剪/生命周期/别名/合并各解决什么 |
| D4 | 09-21 | L3 下 | RG API 设计原因 + Render Graph Viewer 实操 | 在 Viewer 里指出自己管线哪条机制生效了 |
| D5 | 09-22 | L4 | 填 pass 表 + 画完整时序图（**周验收物**） | 时序图能讲给别人听 |
| D6 | 09-23 | — | 补欠账 + 写 200 字周记 | 欠账清零或明确记为债务 |

---

## 📍 今天 · Day 1（L1 SRP 执行流程）

**今日目标**：不是全懂，是画出一条主干时序。
**今日判据**：敲完之后能说清 `Submit()` 为什么必须在最后。说不清就回头重看 Command Buffers 那节。

### 动线

- [ ] **0.5h** 读 Unity 官方那页 simple render loop，建立骨架
- [ ] **3–4h** Catlike Custom SRP **只读第 1 章**，从头敲到 Drawing Geometry
  - [ ] 创建 Pipeline Asset 和 Instance → 知识点①
  - [ ] Camera Renderer / Command Buffers → 知识点②（先录后交）
  - [ ] Culling → 知识点③
  - [ ] Drawing Geometry（DrawingSettings 那段）→ 知识点④
- [ ] **剩余时间** 打开 URP 源码 + Frame Debugger 对照验证
- [ ] 记录主干时序节点（草稿即可，D5 再成图）

**第 1 章里可快速扫过的**：多相机、Editor 里画 Gizmos、不支持的 shader 显示粉色。
**第 2 章 Draw Calls 今天绝对不碰**，那是后面几天的事，翻它只会打乱节奏。
**⚠️ 把网页翻译关掉，读英文原文**。翻译会把 pass 译成"通行证"，draw call / batch / culling 同样会被毁掉，被译文带着误解概念比读英文更累。

### 环境（已确认，2026-09-18）

- **Unity 6.3 LTS (6000.3.9f1)** → **URP 17.3.0**，主路径就是 RenderGraph，Compatibility Mode 已不是默认。
- 你真正要读的项目是 **`F:\unity\TestProject`**（`Assets/01/` 下有 `BlurRendererFeature.cs` / `BlurRenderPass.cs` / `Blur.shader`）。`F:\unity\20Week` 是个空模板工程，别在那读。
- 你的 Blur pass **已经是 RenderGraph 写法**（`public override void RecordRenderGraph(RenderGraph, ContextContainer frameData)`），所以 L3 不是"以后要迁移的新东西"，是你现在跑的那条路。
- 你的 feature 当前挂在 `RenderPassEvent.AfterRenderingSkybox`，Material 在 `Create()` 里建 —— 第 1 周要回答的就是：**为什么是这一格**。

### 源码：怎么打开

源码根目录（TestProject）：

```
F:\unity\TestProject\Library\PackageCache\com.unity.render-pipelines.universal@7327e77c1cc2\Runtime\
F:\unity\TestProject\Library\PackageCache\com.unity.render-pipelines.core@04ab0eefa0c3\Runtime\
```

> Unity 6 的 PackageCache 目录名是哈希后缀（不是版本号），上面两串就是 URP 17.3.0 和 Core 17.3.0。

- 最省事：Unity Project 窗口最下面 → Packages → Universal RP → Runtime，双击任意 `.cs`
- 要全局搜索（推荐，读源码必须能搜）：用 VS Code / Rider 打开上面两个 Runtime 文件夹
- 更自然的入口：打开 `Assets/01/BlurRenderPass.cs`，在 `ScriptableRenderPass` 上按 F12 跳进去，再顺着往上找谁调用它。从自己的代码出发比从头翻更不容易迷路。

### 源码：看哪几个，按这个顺序（已按 17.3 校对，行号为参考）

| # | 问题 | 文件 | 定位 |
|---|---|---|---|
| ① | 谁创建了管线实例 | `Runtime/Data/UniversalRenderPipelineAsset.cs` | `protected override RenderPipeline CreatePipeline()`（约 L811） |
| ② | 每帧入口在哪、Submit 在哪 | `Runtime/UniversalRenderPipeline.cs` | `protected override void Render(ScriptableRenderContext, List<Camera>)`（约 L442）→ `RenderCameraStack`（约 L948）→ `RenderSingleCameraInternal`（约 L689）；搜 `Submit` 看它相对位置 |
| ③ | culling 在整帧的什么位置 | 同上 | `TryGetCullingParameters`（约 L723）→ `data.cullResults = context.Cull(ref cullingParameters)`（约 L850）。注意它在 renderer setup 之前还是之后 |
| ④ | RenderGraph 什么时候被录制 | `Runtime/UniversalRenderPipeline.cs` L902 `RecordAndExecuteRenderGraph(...)` → `Runtime/UniversalRenderPipelineRenderGraph.cs` L8 | 这一步是"录制"与"执行"的分界，**L3 的核心入口** |
| ⑤ | **你的 pass 在哪一格**（第 1 周核心） | `Runtime/ScriptableRenderer.cs` | `AddRenderPasses(ref RenderingData)`（L1650，内部 L1667 逐个调 `rendererFeatures[i].AddRenderPasses(this, ...)`）→ `EnqueuePass()`（L1547）→ `RecordRenderGraph()`（L1243，里面先 `SortStable(m_ActiveRenderPassQueue)` 再 `OnRecordRenderGraph`）→ `SortStable()`（L2377，按 `RenderPassEvent` 排序，**这就是答案**） |
| ⑥ | 内置 pass 的实际顺序 | `Runtime/UniversalRendererRenderGraph.cs` | `OnRecordRenderGraph`（L588）→ `OnBeforeRendering`（L733）/ `OnMainRendering`（L992）/ `OnAfterRendering`（L1353）。你的 `AfterRenderingSkybox` 落在哪一段，在这里能看见 |
| ⑦ | DrawingSettings 实际填了什么 | `Runtime/Passes/DrawObjectsPass.cs` | 看 shader tag、`perObjectData` 开了哪些位 |

> ⚠️ 两个和"旧教程/旧回答"不一样的地方，别找错：
> 1. **`RenderPipelineManager.cs` 不在 core 包里**（Unity 6 已挪进引擎内置模块，C# 源码看不到）。所以①从 `UniversalRenderPipelineAsset.CreatePipeline()` 起步，不要去 core 包翻 `DoRenderLoop_Internal`。
> 2. **`ScriptableRenderer.Execute()` 不再是主路径**，17.3 走 `RecordRenderGraph()`。看到教程说"读 Execute"，对应到 `RecordRenderGraph` 就行。

**读法**：第一天别逐行读、也别往下钻。从 `UniversalRenderPipeline.Render()` 走到 `OnMainRendering`，把路上的关键节点按顺序记下来。遇到 XR、多相机堆叠、Deferred、Offscreen depth 分支直接跳过 —— 要的是主干时序，不是完整理解。
**读够了的标准**：能指着 `ScriptableRenderer.SortStable` 和 `UniversalRendererRenderGraph.OnMainRendering` 说出"我的 Blur pass 在这一行之后、那一行之前执行"。


---

## Week 1 周记（D6 填，200 字）

**这周搞懂了什么**：

**还有什么没搞懂**：

---

## 附：心态基线

看不懂是常态，不是阶段。工作五年的 TA 打开一篇 SIGGRAPH 论文照样看不懂。区别不在懂不懂，而在能不能判断"这个我现在不需要懂"。
状态不好就歇一天，计划里本来就留了 D6 缓冲。硬撑着看不进去，只会加深"我不行"的感觉。
