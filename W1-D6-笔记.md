# Week 1 · Day 6 笔记（2026-09-24 · 补欠账 + 周记）

**今日判据**：欠账清零，或明确记为债务。
**建议投入 2–2.5h**（缓冲日，别加码；W2 明天开始）
**W1 验收物已于 D5 完成**：时序图 ✅ + 9 行 pass 表 ✅

---

## A · 重讲两句（10 分钟）

**A1** 我的 Blur 为什么没被合并？（**不准提 tile 尺寸**）

>原因1 类型是UNsafe不符合合并类型

**A2** `_BlurTexture` 的生命周期 —— 从哪来、到哪去、活多久？

>生命周期就是模糊的两个节点 从第一个模糊节点创建 作为写入 然后第二个模糊节点 作为读取然后就销毁

**A3** 口头纠正三条（写一行即可）：

- 数据流源头是谁：main light shadowmap
- 四条机制各是什么状态：cull看不到 生命周期实现了举例就是这个_BlurTexture纹理的生命周期 内存别名(内存峰值处)理没有证据 pass合并为一个nativepass opaque objects和skybox合可以证明
- Blur 两个 shader pass 谁在前：Vertical在前

---

## B · 累积欠账

### B1 ✅ 已完成：Week10 练习工程三个小改动

- `DrawSkybox` 已挪到 `DrawRenderers` 之后（:61 → :65）
- 拼错的空方法 `DrawVisiblaGeometry()` 已删
- `SortingSettings` 已加 `criteria = SortingCriteria.CommonOpaque`（:53）

### B2 源码① —— 谁创建管线实例（20 分钟）

`Runtime/Data/UniversalRenderPipelineAsset.cs:811`

```
protected override RenderPipeline CreatePipeline()
```

- 它返回什么：返回一个pipeline对象
- 谁调用它、什么时候调：UnversalRenderPipeline调用它 看不到什么时候调用的因为被封装到c++内部了
- 和 D1 我手写的 `CustomRenderPipelineAsset.CreatePipeline()` 有什么不同：
- 补充:  
1. new UniversalRenderPieline(this)中的那个this Asset把自己交给renderpipelin这样创建的Pieline可以直接读取到全部设置，而我的D1没有参数，所以什么都读不到
2. UniversalRednerAsset.CraetePipeline 最后retuen返回给 引擎直接交给底层c++这也解释了为什么没有调用函数但是直接生效了
3. 手写的不传this的原因，因为我写的Asset中没有任何字段存储 所以是个空壳Pipeline拿到也没用所以不传
### B3 D2 复习清单第 9、10 题重测（5 分钟）

盖住 [W1-D2-笔记.md](W1-D2-笔记.md) 第六节的答案列，只测这两题（上次错的）：

- `(RP x:y)` 是什么？

>x是native pass y是subpass

- 为什么我的 Blur 没有 RP 前缀？

>因为我的blur是unsafe pass只有raster render pass可以合并才能谈的上有没有前缀

### B4 D4 遗留问题：PassData 的生命周期（15 分钟）

> D4 笔记原话：「不明白为什么 PassData 的生命周期是 RenderGraph 管的，层级模糊没有串起来」

- 答案：(我的)我现在不是很确定但是可以试着回答一下，因为在 层4和层5当调用recordrendergraph时才会向graph中添加节点 此时才会用到passdata所以passdata才是RenderGraph管理
- 答案：(修改后补充) 方向是对的，但缺少了关键的「为什么」。三层拆清：
  1. **录制层**：`AddRasterRenderPass<MyPassData>()` 时 RenderGraph 从对象池取一个 PassData 实例传给你填字段，你只填表不 new 不 delete
  2. **编译层**：录制完 → 四条优化 → 如果 pass 被裁剪，PassData 直接回收，SetRenderFunc 不会被调用
  3. **执行层**：未被裁的 pass 按序执行，RenderGraph 把 PassData 喂给 static lambda，用完后回收到对象池（使用静态Lambda避免捕获外部变量产生闭包）
  - **为什么必须 RG 管**：因为 static lambda 不能捕获外部变量，如果你自己 new 一个存外面再闭包捕获 → 执行时可能已失效 + 每帧产生 GC 垃圾 + 框架无法控制释放时机
  - **我的 BlurRenderPass 用的 AddBlitPass** 没有显式 PassData，是因为 AddBlitPass 内部替你封装了一个 BlitPassData，逻辑完全一样

- 如果 RenderGraph 允许你直接在类里写成员变量暂存数据、然后在 Lambda 里闭包捕获它，会引发哪两个严重后果？
  1. GC垃圾: 闭包捕获变量会在堆上分配引用对象，频繁捕获可能产生堆分配，增加GC压力(不等于频繁触发GC)。
  2. 时序错乱与脏数据：录制和执行阶段是分离的。如果有多个相机或后续代码修改了该成员变量，CPU 在调用执行函数时可能读到修改后的值，进而生成错误的渲染命令。

### 概念扫盲
01.GC: (Garbage Colletion 垃圾回收)是C#运行时自定回收"不能再被程序访问对象"所占内存的机制(能不能被回收主要看还能不能被找到，不是要求变量直接指向它)

02.声明、执行、提交是三个不同阶段：
- `OnRecordRenderGraph` 遍历到某个 `ScriptableRenderPass` 时，会调用它的 `RecordRenderGraph`。这里向 Render Graph 登记逻辑 pass 节点、资源读写关系和执行函数；不生成绘制命令。`builder.SetRenderFunc(...)` 这句在此时调用，作用是**登记** lambda，并不是运行 lambda。
- 录图完成后，Render Graph 根据节点声明编译、优化图。轮到未被裁剪的逻辑 pass 时，才在 CPU 上**调用**此前登记的执行函数，向命令缓冲区写入 blit、draw 等渲染命令；被裁剪的 pass 不调用。
- `Submit()` 提交已经安排的渲染命令供渲染循环执行；它不重新登记图节点，也不表示 GPU 在这一行同步画完。

03.为什么区分「节点」和「执行函数」：节点是任务说明，记录读什么、写什么、依赖什么，供 Render Graph 判断裁剪、资源生命周期及合并；执行函数是具体做法，轮到这个 pass 时生成渲染命令。先看说明再决定是否调用函数，才能避免为被裁剪的 pass 做无用工作。

04.`AddBlitPass`、节点和 pass 的关系：
- `AddBlitPass` 是便捷 API：在录图阶段替调用者登记一个 blit 逻辑 pass 及其执行行为。手动写法是 `AddRasterRenderPass(...)` 登记逻辑 pass，再用 `builder.SetRenderFunc(...)` 登记执行函数。**两种写法都在录图阶段登记，优化后才调用执行函数**。
- 我的一个 `BlurRenderPass` 对象在一次 `RecordRenderGraph` 中调用 `AddBlitPass` 两次，于是图中有两个逻辑 pass（两个节点）：Vertical Blur 和 Horizontal Blur。这里「两个节点」就是「两个逻辑 pass」，不是另外再加两个 pass。
- GPU 层面的 native pass 是另一层概念：多个符合条件的逻辑 pass 可以合并为一个 native pass，不能按节点数直接推断 native pass 数。我的 Blur 在 Viewer 中显示为 Unsafe Pass，不能参与该处的 Raster Pass 合并。

> 易混点：执行函数在 CPU 上被调用，生成命令；GPU 之后处理提交的命令。不能说「GPU 延迟到执行阶段读取 C# 成员变量」。每帧有堆分配也不等于每帧触发一次 GC。

### B5 「16×16」这个数字的处理（5 分钟）

D4 笔记 ③ 和 D5 口述里都出现了「tile memory 只有 16×16」。

- **概念方向对**：tile memory 只持有**当前 tile** 的像素，不是全屏 → shader 要随机采样整张图时 tile 里没有数据 → 必须回显存 → 合并断开
- **但「16×16」是没核实过的具体数字**，tile 尺寸因 GPU 而异
- **处理**：把数字删掉，只留机制。要写数字就标「典型值，未核实」

- 已改的位置：W1-D4:194-196; W1-D6 A 部分 A1 原因 2

### B6 注入点没列全（20 分钟）

`UniversalRendererRenderGraph.cs` 里还有几处 `RecordCustom*` 没记进 D2 笔记第三节：

- `OnRecordRenderGraph` 段：723 / 725 / 730 各带什么 event？
- `OnBeforeRendering` 段：841 / 845 各带什么 event？

>OnRecordRenderGraph:723有BeforeRenderingShadow, BeforeRenderingOpaques  
>OnRecordRenderGraph:725有AfterRenderingOpaques, BeforeRenderingTransparents  
>OnRecordRenderGraph:730有AfterRenderingTransparents, AfterRendering

>OnBeforeRendering:841有startEvent和splitEvent
>OnBeforeRendering:845有splitEvent和endEvent
>841/845 动态切分的原因：
>URP需要讲深度拷贝(CopyDepth)推迟到最晚执行以节省宽带，但不能晚于任何需要读深度的pass
>因此 splitEvent(最早读深度定义pass的位置)
>1. startEvent -> splitEvent: 先跑不依赖深度的自定义pass
>2. 中间插槽 CopyDepth生成 _CameraDepthTexture
>3. spiltEvent -> endEvent: 再跑需要深度自定义的pass(确保安全读到有效深度图，不黑屏闪烁)

---

## C · Week 1 周记（200 字）

**这周搞懂了什么**：

>通过梳理手写SRP主干，并进一步追踪URP中pass排序与Render Graph流程 (我D1手写的DrawVisibleGeometryURP将这一段分成了更细致的部分 这些部分进入我画的调度和Render Graph流程。自定义pass可以通过AddRenderPasses -> EnqueuePass加入) 详细研究了RenderGraph核心 并且将自己写过的Blur管线放到了整体的管线中对应 熟悉了Graph系统核心 区分了录制与执行 静态Lambda避免捕获变量产生闭包分配 四条对于pass的优化 熟悉了Frame Debugger工具和Render Viewer工具使用他们查看了pass的合并 九条pass输入输出RT及规格;生命周期 Blur未能合并的原因及部分pass的合并证据和不能合并的原因 还了解部分移动端的优化原理

**还有什么没搞懂**：

>具体的管线内部调用细节，辅助工具看不到的别名证据，还有部分数学原理的回顾。

---

## D · 转入 W2 的债务（清不完的写这儿，不硬撑）

-无
